// streams allow us to process indefinite amounts of data in a "streaming fashion".
// That means that you can handle blocks of data as they arrive.
// Evey block is represented by a buffer and therefore you can imagine a stream as
// a sequence of buffers arriving over time.

// TODO
